"""glyph.evaluation — public re-export package.

All implementation lives under glyph.specialized_workers.evaluation.
This package exists so the rest of the codebase (cli, examples, tests)
can import from the stable glyph.evaluation.* namespace.
"""
